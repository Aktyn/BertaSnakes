{
  "name": "bertasnakes-website",
  "version": "1.0.0",
  "description": "website code for berta snakes game",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:live",
    "build": "echo Compiling website code && tsc -p . && node ./compile",
    "build:live": "nodemon -e ts --watch './ts/**/*.ts' --exec tsc -p .",
    "closure-compile": "node ./compile"
  },
  "author": "Aktyn",
  "license": "UNLICENSED",
  "devDependencies": {
    "google-closure-compiler-js": "^20180610.0.0",
    "nodemon": "^1.18.4",
    "typescript": "^3.1.1"
  }
}
